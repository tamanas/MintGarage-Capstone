@model IEnumerable<MintGarage.Models.GalleryTab.Gallery>

@{
    ViewData["Title"] = "Index";
}

<head>
    <link rel="stylesheet" href="~/css/gallery.css" />
</head>

<body>
    <h1>Index</h1>

        @foreach (var item in Model)
        {
            <div class="img-comp-container">
                <div class="img-comp-img">
                    <img src="~/Images/@Html.DisplayFor(modelItem => item.AfterImage)" alt="@Html.DisplayFor(modelItem => item.AfterImage)" />
                </div>
                <div class="img-comp-img img-comp-overlay">
                    <img src="~/Images/@Html.DisplayFor(modelItem => item.BeforeImage)" alt="@Html.DisplayFor(modelItem => item.BeforeImage)" />
                </div>
            </div>
        }

        <script>
            /*Execute a function that will execute an image compare function for each element with the img-comp-overlay class:*/
            initComparisons();
        </script>
    </body>

